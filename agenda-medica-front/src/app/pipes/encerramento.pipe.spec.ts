import { EncerramentoPipe } from './encerramento.pipe';

describe('EncerramentoPipe', () => {
  it('create an instance', () => {
    const pipe = new EncerramentoPipe();
    expect(pipe).toBeTruthy();
  });
});
